import React from "react";
import Base from "../core/Base";

const Signup = () => {
  return (
    <Base title="Signup page" discription="A page for user to signup">
      <h1>Signup</h1>
    </Base>
  );
};

export default Signup;
